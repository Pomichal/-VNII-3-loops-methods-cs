using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExercisesForeachLoop : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

        // TODO: create a method for every exercise and call it from the Start method
    }

    // 1.
    // Summing elements: Create a List of integers (as a public variable)
    // and use a foreach loop to calculate the sum of all the odd elements in the List.
    // Print the result to the console. Experiment with different values in the List
    //
    // 2. Finding maximum: Create a List of integers and use a foreach loop to find
    // the maximum value in the List. Print the maximum value to the console.
    //
    // 3. Finding minimum: use a foreach loop to find the minimum value in the List.
    // Print the minimum value to the console as well.
    //
    // 4. Finding the average: use a foreach loop to find the average value in the List.
    // Print the average value to the console.
}
